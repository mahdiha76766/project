import { DashCard, DashEmpty } from '@/components/shop/DashboardUI';

export default function Page() {
  return <DashCard title='wishlist'><DashEmpty text='این بخش به‌صورت ماژولار آماده شده و به API متصل می‌شود.' /></DashCard>;
}
