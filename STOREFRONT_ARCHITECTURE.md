# معماری ویترین و تجربه فروشگاه

این سند ساختار فعال فروشگاه را از ورود کاربر تا خرید، پیگیری و خدمات پس از فروش مشخص می‌کند. هدف این معماری، کامل‌کردن مسیر فروش است؛ ظاهر هر بخش می‌تواند مستقل از این ساختار تغییر کند.

## ۱. نقشه مسیر مشتری

1. **ورود و کشف:** صفحه اصلی، جستجوی هدر، دسته‌بندی‌ها و خرید بر اساس نیاز
2. **بررسی:** فهرست محصولات، فیلترها، مرتب‌سازی، صفحه‌بندی و پیشنهادهای ویژه
3. **تصمیم:** صفحه محصول، مشخصات، نوع/وزن، موجودی، قیمت، نظرها، مقالات مرتبط و علاقه‌مندی
4. **خرید:** سبد، کوپن، آدرس، روش ارسال، خلاصه سفارش و پرداخت
5. **پس از خرید:** رسید، سفارش‌های من، وضعیت ارسال، فاکتور، بازگشت و ثبت نظر
6. **پشتیبانی:** مرکز راهنما، FAQ، ارسال، پرداخت، مرجوعی، تضمین کیفیت، قوانین و تماس

## ۲. ساختار صفحه اصلی

| ترتیب | بخش | هدف | وضعیت |
|---|---|---|---|
| ۱ | هدر، جستجو و اعلان | دسترسی سریع به فروشگاه، حساب، علاقه‌مندی و سبد | فعال |
| ۲ | اسلایدر اصلی | معرفی کمپین یا دسته اولویت‌دار از تنظیمات مدیریت | فعال |
| ۳ | نوار اعتماد | نمایش ارسال، اصالت، پرداخت و پشتیبانی | فعال |
| ۴ | موضوعات محبوب | ورود مستقیم از کلیدواژه‌های سئو | فعال |
| ۵ | خرید بر اساس نیاز | خوراکی، موضعی، چندمنظوره و تخفیف‌دار | فعال |
| ۶ | دسته‌بندی‌ها | مرور انواع محصولات از داده‌های مدیریت | فعال |
| ۷ | مجموعه‌های محصول | جدیدترین، ویژه، تخفیف‌دار یا تنظیم مدیر | فعال |
| ۸ | بنرهای فروش | کمپین‌های قابل مدیریت | فعال |
| ۹ | مسیر خرید | توضیح انتخاب، ثبت و ارسال + آمار واقعی فروشگاه | فعال |
| ۱۰ | درباره و مزیت‌ها | معرفی برند و استاندارد خدمات | فعال و قابل ویرایش |
| ۱۱ | تجربه مشتریان | آخرین نظرهای تأییدشده و متصل به محصول | فعال |
| ۱۲ | مجله | محتوای آموزشی متصل به محصولات | فعال |
| ۱۳ | FAQ | پاسخ سریع و اتصال به مرکز راهنما | فعال |
| ۱۴ | دعوت به خرید و فوتر | ادامه مسیر، خدمات، حساب و قوانین | فعال |

## ۳. ساختار فهرست محصولات

پارامترهای فعال صفحه `/products`:

| پارامتر | کاربرد |
|---|---|
| `q` | جستجو در نام، توضیحات و برچسب‌ها با escape امن regex |
| `category` | دسته‌بندی فعال |
| `usage` | نوع مصرف: `EDIBLE`، `TOPICAL`، `BOTH`، `NON_EDIBLE` |
| `minPrice` / `maxPrice` | محدوده قیمت مؤثر پس از تخفیف |
| `stock=1` | فقط محصولات دارای موجودی ساده یا موجودی نوع |
| `discount=1` | فقط محصولات دارای تخفیف ساده یا تخفیف نوع |
| `sort` | جدیدترین، پرفروش، ارزان‌ترین و گران‌ترین |
| `page` | صفحه‌بندی سمت سرور با ۱۸ محصول در هر صفحه |

همه فیلترها در موبایل و دسکتاپ فعال‌اند، فیلترهای انتخاب‌شده قابل حذف جداگانه هستند و در جابه‌جایی صفحات حفظ می‌شوند.

## ۴. صفحات اطلاعات و اعتماد

مرکز `/help` مسیرهای زیر را در اختیار مشتری قرار می‌دهد:

- `/help/shopping-guide` — راهنمای انتخاب و خرید
- `/help/shipping` — ارسال و تحویل
- `/help/returns` — شرایط بازگشت
- `/help/payments` — پرداخت و تراکنش
- `/help/quality` — تضمین کیفیت و اصالت
- `/help/privacy` — حریم خصوصی
- `/help/terms` — قوانین استفاده و سفارش
- `/help/faq` — پرسش‌های پرتکرار

این صفحات در هدر، فوتر، صفحه اصلی، صفحه محصول و sitemap به مسیرهای فروشگاه متصل شده‌اند.

## ۵. قابلیت‌های عملیاتی موجود و متصل

- احراز هویت و حساب کاربری
- سبد خرید و تعداد محصولات
- کوپن و محاسبه سمت سرور
- آدرس‌ها و روش‌های ارسال
- پرداخت آنلاین، کارت‌به‌کارت و رسید
- سفارش، فاکتور و پیگیری وضعیت
- کیف پول و تراکنش‌ها
- علاقه‌مندی و افزودن از صفحه محصول
- نظر محصول و تأیید مدیریت
- درخواست بازگشت
- محصولات چندنوعی و کنترل موجودی هر نوع
- مدیریت محصول، دسته، بنر، اسلایدر، محتوا، ارسال و تنظیمات
- SEO، داده ساختاریافته، sitemap و تحلیل رفتار کاربر

## ۶. قواعد نگهداری ساختار

- هیچ فیلتر یا قیمت نهایی نباید فقط در کلاینت محاسبه شود.
- بخش‌های داده‌محور باید در نبود دیتابیس fallback امن داشته باشند.
- لینک نمایشی بدون مقصد یا دکمه بدون عملکرد نباید در ویترین باقی بماند.
- صفحات خصوصی حساب، پرداخت و سفارش نباید در sitemap قرار گیرند.
- محتوای پزشکی باید اطلاع‌رسانی عمومی باشد و جایگزین توصیه متخصص معرفی نشود.
- افزودن بخش جدید به صفحه اصلی باید یک هدف روشن در مسیر کشف، تصمیم یا اعتماد داشته باشد.
